import React from 'react'

const ConditionalRendering = () => {
  return (
    <div>
      &&-short circuit, ? : -ternary conditional operator, if else, variables
    </div>
  )
}

export default ConditionalRendering
